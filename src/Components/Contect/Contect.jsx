import React from 'react'
import css from './Contect.module.scss'
export const Contect = () => {
  return (
   <section className={`paddings ${ css.warpper }`}>
   

<div className={`flexCenter innerWidth ${ css.container }`}>
    hi
    </div>
    </section>
  )
}
